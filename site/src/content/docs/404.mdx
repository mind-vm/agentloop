---
title: Page not found
description: That page does not exist. Here is where the rest of the documentation lives.
template: splash
editUrl: false
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
hero:
  title: "404"
  tagline: >
    That page doesn't exist. It may have moved, or the link that brought you
    here may be stale.
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";
import { base } from "../../../site.config.mjs";

export const url = (path) => `${base.replace(/\/+$/, "")}/${path}`;

{/*
  These hrefs are absolute and built from `base`, unlike every other page,
  which writes plain Markdown links and lets rehype-base-links.mjs add the
  prefix. Two reasons it has to be done by hand here.

  This page is served for *any* unmatched path, so a relative link would
  resolve against whatever wrong URL the reader actually hit rather than
  against the site root. And the rehype plugin only rewrites Markdown links —
  the JSX below produces a different node type that it deliberately skips.
*/}

<CardGrid>
  <LinkCard
    title="Quickstart"
    href={url("start/")}
    description="Install agentloop, wire up a loop with in-memory stores, and run the example CLI."
  />
  <LinkCard
    title="Concepts"
    href={url("concepts/")}
    description="Why the model writes JavaScript instead of calling named tools, and what each package does."
  />
  <LinkCard
    title="Extending"
    href={url("extending/")}
    description="Capabilities, sandbox composition, your own stores, and the policy seam."
  />
  <LinkCard
    title="Reference"
    href={url("reference/")}
    description="Known limitations, license, and the full API on pkg.go.dev."
  />
</CardGrid>

Think something should be here?
[Open an issue](https://github.com/mind-vm/agentloop/issues).
